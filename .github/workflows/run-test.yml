name: Run test suite
on: [push]

jobs:
  run-tests:
    name: Run tests
    runs-on: ubuntu-latest
    container:
      image: postgres:latest
    steps:
      - name: Testes
        run: echo "AAA"
